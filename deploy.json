{
    "deploy": [
    {
      "name": "Backup",
      "icon": "cloud-upload",
      "command": "DATE=$(date +%F); DATETIME=$(date +%F_%H-%M-%S); mkdir -p /srv/backups/$DATE && 7z a \"/srv/backups/$DATE/BKP_${DATETIME}.7z\" /srv/sites/ubvaia -xr!node_modules"
    },
    {
      "name": "Start",
      "icon": "play",
      "command": "bun run dev:turbo"
    }
  ]
}